const express = require("express");
const { setPosts, getPosts, editPost, deletePost, likePost } = require("../../controllers/post.controller");

const router = express.Router();

router.get("/",getPosts);
   
router.post("/",setPosts);

router.put("/:id",editPost); //=>{
   // res.json({ message:"message supprimé:"+req.params.id});
//});

router.delete("/:id",deletePost);// =>{
   // console.log("message supprimé");
    //res.json({message:"message supprimé:"+req.params.id});
//});
   router.patch("/like-post/:id",likePost); // =>{
    // res.json({message:"post liké:"+req.params.id});
// })
// router.patch("/dislike-post/:id",(req, res) =>{
// res.json({message:"post disliké:"+req.params.id});
// })


module.exports = router;